{
  "name": "dutybell",
  "version": "0.1.0",
  "description": "Fail-closed, offline-first reliability layer for scheduled tasks: overlap policies (skip/queue/run), retries with backoff, append-only JSONL ledger, catch-up windows, webhook notifications, drift detection — all state is auditable plain text, invoked by host cron once per minute.",
  "type": "module",
  "bin": {
    "dutybell": "dist/tools/dutybell.js"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run",
    "build": "node build.mjs",
    "verify": "npm run check && npm test && npm run build"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/node": "^26.2.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "license": "MIT"
}
